{
  "name": "raken",
  "version": "1.0.0",
  "description": "A sleek web application that analyzes and sorts Spotify playlists by mood using audio feature analysis.",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "build": "cd client && npm run build && cd ../server && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
